import styles from "./styles.module.scss";

const Footer = () => {
	return (
		<footer className={styles.footer}>
			<div>This is a test project created by Sohail</div>
		</footer>
	);
};

export default Footer;
